<Window x:Class="Tagliste.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:Tagliste.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    
    <Window.DataContext>
        <viewModel:MainWindowViewModel />
    </Window.DataContext>
    
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <Button Margin="10" Padding="10" Click="CreateTag_OnClick">Add</Button>
            <Button Margin="10" Padding="10">Button</Button>
            <Button Margin="10" Padding="10">Yet another button</Button>
        </StackPanel>
        <DataGrid 
            x:Name="DataGrid" 
            ItemsSource="{Binding Path=Tags}" 
            ColumnWidth="*" 
            CanUserAddRows="False"
            AutoGeneratedColumns="DataGrid_OnAutoGeneratedColumns">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Edit/Delete">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate >
                            <StackPanel Orientation="Horizontal">
                            <Button Click="DeleteTag_OnClick">Delete</Button>
                            <Button Click="EditTag_OnClick">Edit</Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
